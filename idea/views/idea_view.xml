<?xml version="1.0"?>
<odoo>
    <!-- Top menu item -->
    <menuitem name="Idea" id="root_idea" sequence="120" groups="group_idea_user"/>
    <menuitem name="Ideas" parent="root_idea" id="idea_idea" sequence="20" />
    <menuitem name="Configuration" parent="root_idea" id="idea_configuration" sequence="20" />

    <!-- Idea Categories Search View-->
    <record model="ir.ui.view" id="view_idea_category_search">
        <field name="name">idea.category.search</field>
        <field name="model">idea.category</field>
        <field name="arch" type="xml">
            <search string="Ideas Categories">
                <field name="name" string="Category"/>
            </search>
        </field>
    </record>

    <!-- Idea Category Form View -->
    <record model="ir.ui.view" id="view_idea_category_form">
        <field name="name">idea.category.form</field>
        <field name="model">idea.category</field>
        <field name="arch" type="xml">
            <form string="Category of Ideas">
                <group>
                    <field name="name"/>
                </group>
            </form>
        </field>
    </record>

    <!-- Idea Category Tree View -->
    <record model="ir.ui.view" id="view_idea_category_tree">
        <field name="name">idea.category.tree</field>
        <field name="model">idea.category</field>
        <field name="arch" type="xml">
            <tree string="Category of ideas">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- Idea Category Action -->

    <record model="ir.actions.act_window" id="action_idea_category">
        <field name="name">Categories</field>
        <field name="res_model">idea.category</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_idea_category_search"/>
    </record>

    <menuitem name="Categories" parent="idea_configuration" id="menu_idea_category" action="action_idea_category" />

    <!-- New Idea Form View -->

    <record model="ir.ui.view" id="view_idea_idea_form">
        <field name="name">idea.idea.form</field>
        <field name="model">idea.idea</field>
        <field name="arch" type="xml">
            <form string="Idea">
                <header>
                    <button name="idea_open" type="object" string="Open" states="draft" class="oe_highlight"/>
                    <button name="idea_close" type="object"  string="Accept" states="open" class="oe_highlight"/>
                    <button name="idea_cancel" type="object"  string="Refuse" states="open"  class="oe_highlight"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,open,close"/>
                </header>
                <sheet>
                    <label for="name" class="oe_edit_only"/>
                    <h1><field name="name"/></h1>
                    <group>
                        <group>
                            <field name="category_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                        </group>
                    </group>
                    <separator/>
                    <label for="description"/>
                    <field name="description"/>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
            </form>
        </field>
    </record>

    <!-- New Idea Tree View -->

    <record model="ir.ui.view" id="view_idea_idea_tree">
        <field name="name">idea.idea.tree</field>
        <field name="model">idea.idea</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="create_uid"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <!-- Search Idea  -->

    <record model="ir.ui.view" id="view_idea_idea_search">
        <field name="name">idea.idea.search</field>
        <field name="model">idea.idea</field>
        <field name="arch" type="xml">
            <search string="Ideas">
                <field name="name" string="Idea"/>
                <filter name="new" string="New" domain="[('state','=', 'draft')]" help="New Ideas"/>
                <filter name="inprogress" string="In Progress" domain="[('state','=', 'open')]" help="Open Ideas"/>
                <filter name="accepted" string="Accepted" domain="[('state','=','close')]" help="Accepted Ideas" />
                <field name="category_ids"/>
                <group name="groupby_idea" expand="0" string="Group By...">
                    <filter name="creator" string="Creator" help="By Creators" context="{'group_by':'create_uid'}"/>
                    <filter name="status" string="Status" help="By States" context="{'group_by':'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record model="ir.actions.act_window" id="action_idea_idea">
        <field name="name">Ideas</field>
        <field name="res_model">idea.idea</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_idea_idea_search"/>
    </record>

    <menuitem name="Ideas" parent="idea_idea" id="menu_idea_idea" action="action_idea_idea" sequence="1"/>

</odoo>
